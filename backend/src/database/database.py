class Database:

   def __init__(self, user, passwd, host, database):
      self.user = user
      self.passwd = passwd
      self.host = host
      self.database = database

   def connect(self):
      pass
